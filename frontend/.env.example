# Server-only backend URL; no secrets in NEXT_PUBLIC_ variables.
API_BASE_URL=http://localhost:5081
APP_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
# false only for local HTTP; true for HTTPS deployment.
AUTH_COOKIE_SECURE=false
